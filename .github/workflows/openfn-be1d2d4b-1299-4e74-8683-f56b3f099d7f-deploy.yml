on:
  push:
    branches:
      - master

jobs:
  deploy-to-lightning:
    runs-on: ubuntu-latest
    name: A job to deploy to Lightning
    steps:
      - name: openfn deploy
        uses: midigofrank/cli-deploy-action@v0.1.12
        with:
          secret_input: ${{ secrets.OPENFN_be1d2d4b_1299_4e74_8683_f56b3f099d7f_API_KEY }}
          config_path_input: 'openfn-be1d2d4b-1299-4e74-8683-f56b3f099d7f-config.json'
